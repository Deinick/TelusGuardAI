# Loaded automatically by `vite build` (production mode).
# No backend is deployed alongside the static GitHub Pages build, so the app
# runs in demo mode: the map and tower KPIs are fully self-contained, and the
# natural-language "Run analysis" button loads the bundled sample scenario
# instead of calling a live backend.
VITE_DEMO_MODE=true
